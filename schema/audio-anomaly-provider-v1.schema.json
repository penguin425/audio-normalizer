{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://penguin425.github.io/audio-normalizer/schema/audio-anomaly-provider-v1",
  "title": "Forge external audio anomaly provider input v1",
  "description": "Bounded, provenance-bound findings from an optional external audio-quality model.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "provider",
    "provider_version",
    "model",
    "model_version",
    "model_sha256",
    "source_sha256",
    "source_duration_seconds",
    "events"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "provider": {"type": "string", "minLength": 1},
    "provider_version": {"type": "string", "minLength": 1},
    "model": {"type": "string", "minLength": 1},
    "model_version": {"type": "string", "minLength": 1},
    "model_sha256": {"$ref": "#/$defs/sha256"},
    "source_sha256": {"$ref": "#/$defs/sha256"},
    "source_duration_seconds": {"type": "number", "exclusiveMinimum": 0, "maximum": 604800},
    "sample_rate_hz": {"type": ["integer", "null"], "minimum": 1},
    "events": {
      "type": "array",
      "maxItems": 100000,
      "items": {"$ref": "#/$defs/event"}
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[A-Fa-f0-9]{64}$"
    },
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "start_seconds",
        "end_seconds",
        "confidence",
        "severity"
      ],
      "properties": {
        "kind": {
          "enum": ["noise", "pop", "dropout", "lip-noise", "phase-cancellation", "clipping", "other"]
        },
        "start_seconds": {"type": "number", "minimum": 0},
        "end_seconds": {"type": "number", "exclusiveMinimum": 0},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "severity": {"type": "number", "minimum": 0, "maximum": 1},
        "channel": {"type": ["integer", "null"], "minimum": 1, "maximum": 65535},
        "related_channel": {"type": ["integer", "null"], "minimum": 1, "maximum": 65535},
        "evidence_label": {"type": ["string", "null"], "minLength": 1, "maxLength": 128}
      }
    }
  }
}
